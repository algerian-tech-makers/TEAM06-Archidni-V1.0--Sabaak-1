import 'package:flutter/material.dart';

class AppSchoolTitel extends StatelessWidget {
  const AppSchoolTitel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}